# Direct dependencies only — pip resolves the rest.
# Pinned so Render builds are reproducible.

# --- web layer ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- AI ---
google-genai==1.1.0

# --- content -> HTML ---
Markdown==3.7
Pygments==2.18.0

# --- HTML -> PDF (Chromium) + PDF post-processing ---
playwright==1.60.0
PyMuPDF==1.26.5

# --- Neon Postgres (library + stored PDFs) ---
psycopg[binary,pool]==3.2.13
